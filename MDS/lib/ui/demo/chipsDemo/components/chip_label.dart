import 'package:flutter/material.dart';

class ChipLabel extends StatelessWidget {
  const ChipLabel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Label');
  }
}
